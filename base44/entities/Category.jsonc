{
  "name": "Category",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Category display name"
    },
    "emoji": {
      "type": "string",
      "description": "Emoji icon for the category"
    },
    "color": {
      "type": "string",
      "description": "Hex color code for the category badge"
    },
    "slug": {
      "type": "string",
      "description": "URL-friendly slug"
    },
    "subtitle": {
      "type": "string",
      "description": "Short description shown on tile"
    },
    "hero_image_url": {
      "type": "string",
      "description": "Optional override for the tile background photo. Leave empty to use the built-in default. Supports any HTTPS image URL (Unsplash, custom CDN, base44 file storage)."
    }
  },
  "required": [
    "name",
    "emoji",
    "color",
    "slug"
  ]
}